FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production
COPY package.json pnpm-lock.yaml* ./
RUN corepack enable && pnpm install --prod --frozen-lockfile
COPY deploy/zkml-worker.mjs ./zkml-worker.mjs
USER node
EXPOSE 8090
CMD ["node", "zkml-worker.mjs"]
